#!/usr/bin/python3
# match the bytecode as provided in the question by alx


def magic_calculation(a, b, c):
    if a < b:
        return (c)
    if c > b:
        return (a + b)
    return (a*b - c)

